import type { ReactNode } from 'react'
import { Table, TableBody, TableCell, TableHead, TableHeader, TableRow } from '../ui/table'
export interface TableColumn<T> { key: string; header: string; render: (row: T) => ReactNode }
export function AppTable<T>({ columns, data, rowKey, onRowClick }: { columns: TableColumn<T>[]; data: T[]; rowKey: (row: T) => string; onRowClick?: (row: T) => void }) { return <div className="overflow-hidden rounded-2xl border border-border/90 bg-card shadow-[0_1px_2px_rgba(15,23,42,.035),0_10px_28px_rgba(15,23,42,.035)]"><Table className="min-w-[760px]"><TableHeader><TableRow>{columns.map((column) => <TableHead key={column.key}>{column.header}</TableHead>)}</TableRow></TableHeader><TableBody>{data.map((row) => <TableRow key={rowKey(row)} onClick={() => onRowClick?.(row)} className={onRowClick ? 'cursor-pointer focus-within:bg-primary/[.035]' : ''}>{columns.map((column) => <TableCell key={column.key}>{column.render(row)}</TableCell>)}</TableRow>)}</TableBody></Table></div> }
